{
  "name": "surge-pos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd web && pnpm install --frozen-lockfile && pnpm build:no-bench",
    "dev": "cd web && pnpm dev"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
