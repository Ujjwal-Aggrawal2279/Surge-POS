{
  "name": "surge-pos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm install -g pnpm@9 --quiet 2>/dev/null; cd web && pnpm install --frozen-lockfile && pnpm build:no-bench",
    "dev": "cd web && pnpm dev"
  },
  "engines": {
    "node": ">=18"
  }
}
