# High-performance serialization (Rust-core)
orjson>=3.10.0          # 10x faster JSON than stdlib — written in Rust
msgspec>=0.18.0         # Faster than Pydantic, Rust-core encoder/decoder

# Async HTTP (for Pine Labs, payment gateways)
httpx>=0.27.0

# Fast Redis client (C extension)
redis[hiredis]>=5.0.0   # hiredis = C parser, 10x faster Redis reads

# Fast event loop (Rust-based libuv bindings)
uvloop>=0.19.0          # Drop-in asyncio replacement, near C speed

# In-process LRU cache for ultra-hot data
cachetools>=5.3.0
